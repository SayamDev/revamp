# Screenshots

Captured from the deployed demo at <https://sayamdev.github.io/relay/> with
Playwright, at 1440×940. They are real screens from the running application,
not mockups.

To regenerate them after a UI change, drive the deployed (or local) app with a
headless browser and overwrite the files in this directory.
